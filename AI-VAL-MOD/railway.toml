[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn Validation-Module.app.main:app --host 0.0.0.0 --port $PORT --workers 1"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
