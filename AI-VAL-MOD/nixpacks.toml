[phases.setup]
nixPkgs = ["python311", "gcc", "libstdcxx5"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "uvicorn Validation-Module.app.main:app --host 0.0.0.0 --port $PORT --workers 1"
